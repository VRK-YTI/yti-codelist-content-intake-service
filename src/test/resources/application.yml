---
spring:
  profiles: automatedtest
  jpa:
    generate-ddl: false
    database-platform: "org.hibernate.dialect.PostgreSQL9Dialect"
    properties:
      hibernate:
        ddl-auto: none
        jdbc:
          lob:
            non_contextual_creation: false

thread:
  pool:
    idle-timeout: 10000
    max: 50
    min: 10

groupmanagement:
  url: https://rhp.dev.yti.cloud.vrk.fi
  publicUrl: https://rhp.dev.cloud.vrk.fi

terminology:
  url: https://sanastot.dev.cloud.vrk.fi
  publicUrl: https://sanastot.dev.cloud.vrk.fi

datamodel:
  publicUrl: https://tietomallit.dev.cloud.vrk.fi

comments:
  publicUrl: https://kommentit.dev.cloud.vrk.fi

messaging:
  url: http://localhost:9801

service:
  name: yti-codelist-content-intake-service
  database-name: yti_codelist

hikari:
  jdbcUrl: jdbc:postgresql://${environment.postgres.host}:${testpostgres.port}/${service.database-name}
  username: postgres
  password:
  minimumIdle: 10
  maximumPoolSize: 300

yti_codelist_content_intake_service_elastic_host: ${environment.elastic.host}
yti_codelist_content_intake_service_elastic_port: ${testelasticsearch.http.port}

environment:
  elastic:
    host: 127.0.0.1
    port: 9200
  postgres:
    host: 127.0.0.1
    port: 5432

server:
  port: 9602

application:
  scheme: http
  host: localhost
  port: 9602
  contextPath: /codelist-intake
  access-log: true

publicapi:
  scheme: http
  host: localhost
  port: 9601
  contextPath: /codelist-api

urisuomi:
  scheme: http
  host: uri.suomi.fi
  contextPath: /codelist

session:
  timeout-mins: 10
